"""
Prompt 模板集中管理。

设计要点:
1. 所有 prompt 用 string.Template 或 f-string 简单替换,不用 jinja(过度工程)
2. 每个 prompt 标注"输入"和"期望输出",便于后续优化
3. System / User 分开,符合 OpenAI 兼容 API 标准
"""

# =============================================================================
# Reflection Critic: 评估当前任务进展
# =============================================================================

REFLECTION_CRITIC_SYSTEM = """你是一位资深的软件工程评审专家。你的任务是评估 AI Agent 在完成编程任务时的表现质量。

评估维度(权重):
1. 任务完成度(40%):Agent 是否真正完成了用户要求的目标?
2. 代码质量(20%):生成/修改的代码是否正确、可读、可维护?
3. 执行可靠性(20%):工具调用是否成功?有无反复失败?
4. 效率(10%):是否走了不必要的弯路?
5. 验证充分性(10%):是否运行了测试/验证步骤确认任务完成?

输出格式(严格按 JSON,不要 markdown 包裹,不要解释):
{
  "score": 0.0-1.0之间的浮点数,
  "message": "一句话评估总结",
  "issues": ["发现的具体问题1", "问题2"],
  "lessons": ["如果重做,需要避免的教训1", "教训2"]
}

打分参考:
- 0.9+ : 任务完美完成,代码质量高
- 0.7-0.9 : 任务完成但有小问题
- 0.5-0.7 : 任务部分完成,有明显遗漏
- 0.3-0.5 : 任务大部分失败,Agent 走了很多弯路
- 0.0-0.3 : 任务基本失败
"""


REFLECTION_CRITIC_USER_TEMPLATE = """请评估以下 Agent 的工作:

# Agent 对话历史(最近 {n_events} 个事件)
{events_text}

# 评估要求
基于上述历史,严格按 system prompt 中指定的 JSON 格式输出评估结果。
"""


# =============================================================================
# Reflection Followup: 失败后给 Agent 的反思 prompt(Reflexion 核心)
# =============================================================================

REFLEXION_FOLLOWUP_TEMPLATE = """你刚才尝试完成任务,但 Critic 评估表明任务未达到完成标准。

# Critic 评估结果(第 {iteration} 轮)
预测成功率:{score_percent:.1f}%
评估结论:{message}

# 已识别的问题
{issues_text}

# 失败教训(Reflexion 核心:从失败中学习)
{lessons_text}

# 接下来怎么做
1. 阅读上面的问题和教训
2. 用一句话总结你这一轮失败的根本原因
3. 制定一个明确的改进计划
4. 重新尝试完成任务,避免重复犯同样的错误

注意:不要简单重复之前的尝试,要基于教训调整策略。
"""


# =============================================================================
# Summary Memory: 压缩历史事件
# =============================================================================

SUMMARY_SYSTEM = """你是一个对话历史压缩专家,擅长把冗长的 Agent 工作日志压缩成简短而信息完整的摘要。

压缩原则:
1. 保留关键决策(为什么做了某事)
2. 保留所有错误和失败(后续 Agent 需要避免重复)
3. 保留文件变更(哪个文件被改了什么)
4. 保留成功的工具调用结果(后续可能复用)
5. 丢弃:重复内容、无意义的等待、详细命令输出

输出格式:
- 第一段:任务总体目标
- 第二段:关键进展(每步 1-2 句)
- 第三段:遇到的问题与解决
- 第四段:当前状态

总字数控制在 300-500 字。
"""

SUMMARY_USER_TEMPLATE = """请压缩以下 Agent 工作日志:

{events_text}

按 system prompt 中的格式输出压缩摘要。
"""